defaults:
  - dataset: "audio_mnist"
  - _self_

experiment_name: "audio_mnist_baseline"

dataloader:
  batch_size: 32
  num_workers: 4
  shuffle: true

train:
  seed: 42
  epochs: 10
  learning_rate: 0.001
  device: cpu
